// Deprecated entry point, kept only so nothing that imports './index.js' directly breaks.
// Use server.js for local dev, and api/index.js (Vercel) for serverless deployment.
export { default } from './app.js';
